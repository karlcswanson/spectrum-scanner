{
  "name": "micboard-spectrum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:standalone": "BUILD_MODE=standalone vite",
    "build:standalone": "BUILD_MODE=standalone vite build",
    "build:scanner": "BUILD_MODE=standalone vite build && rm -rf ../scanner/internal/api/web/assets && cp -r dist-standalone/assets ../scanner/internal/api/web/ && cp dist-standalone/standalone.html ../scanner/internal/api/web/index.html && cp dist-standalone/logo.png ../scanner/internal/api/web/ && cp dist-standalone/favicon.png ../scanner/internal/api/web/",
    "build:all": "npm run build && npm run build:scanner"
  },
  "dependencies": {
    "@vueuse/core": "^14.1.0",
    "d3": "^7.9.0",
    "mqtt": "^5.14.1",
    "pinia": "^2.1",
    "vue": "^3.4",
    "vue-router": "^4.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0",
    "autoprefixer": "^10.4",
    "postcss": "^8.4",
    "tailwindcss": "^3.4",
    "vite": "^5.0"
  }
}
